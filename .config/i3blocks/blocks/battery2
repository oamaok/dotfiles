#/bin/bash
PERCENTAGE=$(acpi | sed -E 's/.* ([0-9]+)%.*/\1/g')

echo $(acpi | sed -E 's/.* (\w+), ([0-9]+)%, ([0-9:]+) .*/\2%, \1 (\3)/g')

if [[ $PERCENTAGE -lt 15 ]]; then
  play /home/teemu/.config/i3blocks/blocks/wilhelm.wav > /dev/null 
fi